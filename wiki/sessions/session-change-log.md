## 2026-07-10 18:48 EDT

- Completed the lean-agent second pass: consolidated the company north star into [[company/money-machine-360|Money Machine Operating Context]], repaired index/checkpoint routing, reduced mandatory startup context, made chief-of-staff and coding-manager explicit-only, retired overlapping agent roles, and collapsed ten worker/review templates into two optional managed-execution references. Direct execution is now the default; capital safety and invariant-first correctness remain always on. No Linear, runtime, trading, account, strategy lifecycle, deployment, secret, commit, or push action was performed.

## 2026-07-10 02:48 EDT

- Reset the agent operating system around trading revenue: positive net realized live P&L after costs over a founder-set proof period. Added an always-applied revenue-closure rule; changed Linear intake, role skills, worker/coding-manager templates, and KB hygiene to require current-system evidence, no-build alternatives, work deletion, risk-proportional ceremony, and terminal closure instead of automatic tickets/follow-ups. Capital authorization and high-risk correctness gates remain hard constraints. No Linear issues were mutated.

## 2026-07-10 02:06 EDT

- Coding-manager accepted `MON-134` after delta 3 plus a final manager correction to the public/stored identity type boundary. Plain `dict[str, pydantic.JsonValue]` input is validated before recursively frozen storage; unsupported caller containers and malformed non-JSON identity fields are rejected; strict mypy, changed-file compile, IDE lints, and an independent identity smoke passed. Worker baseline: `46` foundation + `144` remaining backtest tests. Sandboxed pytest rerun was blocked by repo `.env` permissions, so secret access was not requested. Committed as `5a659548` (`Add durable backtest persistence foundation`).

## 2026-07-10 01:58 EDT

- Coding-manager re-reviewed MON-134 delta 2: still **Narrow**, not committed. Recursive freezing now blocks nested mutation and stabilizes repeated hashes, but the frozen representation is incorrectly annotated as `pydantic.JsonValue`; strict mypy independently reproduces 2 incompatible-return errors. Caller-supplied tuple and `MappingProxyType` values are also accepted and hashed as arrays/objects instead of rejected as unsupported input; a direct probe showed tuple input hashes identically to list input. Require pre-freeze input validation, an accurate recursive frozen-JSON type, unsupported-type negatives, and green strict mypy.

## 2026-07-10 01:49 EDT

- Coding-manager delta acceptance re-review of `MON-134`: still **Narrow**, not committed. Signed-zero identity, `Any`, BacktestStrategy coverage, full NUMERIC matrix, database cascade proof, and migration restoration were corrected (`40` focused + `144` other backtest tests reported). Remaining invariant bug: only top-level identity mappings are proxied; nested dict/list mutation still succeeds and direct verification changes the same object’s hash. Replacing Pydantic with a dataclass also removed runtime field validation. Require recursive immutable canonical storage, restored runtime validation, and nested-mutation/repeated-hash tests.

## 2026-07-10 01:39 EDT

- Coding-manager deep acceptance review of `MON-134`: **Narrow**, not committed. The additive schema/migration is directionally correct and reported suites passed, but `Decimal("0")` and `Decimal("-0")` currently produce different Variant keys; the identity helper uses prohibited `Any` and is only shallow-frozen. Brief-required tests are incomplete for BacktestStrategy snapshot/deletion, every NUMERIC special value, and database-level cascade behavior; the migration round-trip also needs guaranteed restoration/isolation. Return contained corrections to the worker and require a delta handoff.

## 2026-07-10 01:39 EDT

- Created MON-133 child chain in Linear and attached issue-linked briefs: `MON-135` typed Trial results/attempt artifacts blocked by `MON-134`; `MON-136` durable scheduler/recovery blocked by `MON-135`; `MON-137` API/MCP/frontend compatibility blocked by `MON-136`; `MON-138` saved research/signal-deciles migration blocked by `MON-137`; `MON-139` destructive legacy removal blocked by `MON-138` and blocking `MON-132`. All remain Triage; no downstream worker launch before predecessor acceptance and ticket-local coding-manager review.

## 2026-07-10 01:29 EDT

- Packaged the remaining MON-133 architecture into five dependency-ordered child briefs under `wiki/engineering/`: typed Trial results/attempt artifacts; durable scheduler/lifecycle/recovery; API/MCP/frontend compatibility projections; saved research/signal-deciles migration; and final destructive legacy removal. Each brief locks scope, identities, failure/retry behavior, file ownership, negative tests, anti-goals, stop conditions, and hard predecessor gates. Updated the parent plan and wiki routing; only the results/artifacts child is eligible for launch review after MON-134 acceptance.

## 2026-07-10 01:21 EDT

- Applied the final MON-134 launch-review tightening: corrected the brief header, added named checks binding each present navigational strategy FK to its immutable snapshot, changed `variant_key` to `VARCHAR(64)` with a lowercase SHA-256 hex regex check, and added PostgreSQL negative tests for mismatched navigation/snapshot IDs and malformed hashes. Ready for another launch-review pass; no worker launched.

## 2026-07-10 01:17 EDT

- Tightened the MON-134 foundation brief after coding-manager review: added immutable non-FK `strategy_id_snapshot` / `backtest_strategy_id_snapshot` fields while retaining nullable source FKs only for navigation; canonical hashing now excludes mutable FKs. Added explicit PostgreSQL checks excluding `NaN`/`±Infinity` from every V2 NUMERIC field, `jsonb_typeof(...) = 'object'` checks for all Variant config blobs, and negative tests for both constraint families plus source-deletion identity stability. Ready for launch-review rerun; no worker launched.

## 2026-07-10 01:24 EDT

- Final coding-manager launch review of `MON-134`: **Ready**. Linear now embeds the fully tightened foundation brief (new upload and ticket update confirmed). The additive four-table schema, immutable Variant identity, database constraints, PostgreSQL negative tests, file ownership, downgrade, anti-goal, and stop condition are explicit enough for a cold-start worker. Worker kickoff packaged; broad `MON-133` remains non-worker architecture.

## 2026-07-10 01:23 EDT

- Coding-manager re-review found the updated local `MON-134` brief technically launch-ready: FK/snapshot consistency, digest format, finite NUMERIC, JSON shape, and matching negative tests are explicit. Final decision remains **Tighten** only because Linear still embeds the earlier immutable upload (unchanged ticket timestamp/attachment size), so a cold worker would receive stale instructions. Replace the attachment, then perform the final launch packaging check.

## 2026-07-10 01:19 EDT

- Coding-manager re-reviewed the tightened `MON-134` brief: prior immutable-snapshot, finite-NUMERIC, and JSON-object blockers are resolved; decision remains **Tighten** for one final identity-constraint pass. Add named checks requiring each present navigational strategy FK to equal its immutable snapshot and requiring `variant_key` to be lowercase 64-character SHA-256 hex; add matching PostgreSQL negative tests and replace the stale `MON-TBD` brief header. No worker kickoff yet.

## 2026-07-10 01:15 EDT

- Coding-manager launch review of `MON-134` and its foundation brief: **Tighten**. The slice boundary, file ownership, schema identities, downgrade, and negative constraint coverage are otherwise launchable. Required correction: mutable source FKs using `ON DELETE SET NULL` cannot also be immutable `variant_key` inputs; preserve separate immutable snapshot IDs or define a different stable identity. Also require DB-level finite `NUMERIC` constraints and PostgreSQL negative tests for non-object Variant JSON. No worker kickoff yet.

## 2026-07-10 01:04 EDT

- Applied the MON-133 Stop review tightening: MON-133 stays the non-worker architecture parent. Drafted `wiki/engineering/MON-133-bt-foundation-slice-brief.md` for the only next launchable child—additive `BtStudy` / `BtVariant` / `BtTrial` / `BtTrialAttempt` models, exact constraints, immutable strategy/config identity, pure canonical key helper, and negative contract tests. Split results/artifacts, scheduler/lifecycle, compatibility, research migration, legacy deletion, and grid into ordered follow-on tickets; no implementation launched.

## 2026-07-10 01:01 EDT

- Coding-manager launch review of `MON-133` and attached Backtest Persistence V2 plan: **Stop**. Study → Variant → Trial → Attempt direction and clean-cutover stance are coherent, but the brief is not yet migration-ready or worker-launchable. It still leaves complete metric/artifact DDL, strategy snapshot identity, lifecycle transition/idempotency/concurrency guards, exact failure mapping, compatibility projections, rollback points, bounds/config sources, and launch-reviewable implementation slices/file ownership to worker invention. No implementation launch or kickoff prompt.

## 2026-07-10 00:25 EDT

- Paused `MON-132` after first-principles review found its nested grid parent → cell → single/batch leaf model compounded ambiguity in `BtBacktestRun` / `BtBacktestRunAsset` / batch aggregates. Created high-priority `MON-133` to redesign durable backtest persistence around Study → Variant → Trial, with explicit identity, lifecycle, retry, partial-failure, artifact ownership, compatibility, migration, and rollback contracts. `MON-132` is Blocked by `MON-133`; no schema implementation launches before a coding-manager review.

## 2026-07-10 00:34 EDT

- Expanded [[vendors/binance-market-data-access|Binance Market Data Access]] with spot `/api/v3/klines` request/response schema (12-field array), intervals, `limit` default/max 500/1000, weight 2, live `exchangeInfo` rateLimits (`REQUEST_WEIGHT` 6000/1m, `RAW_REQUESTS` 300000/5m), 429/418 semantics, and futures `/fapi/v1/klines` limit/weight table (max 1500; weight scales with limit).

## 2026-07-09 23:25 EDT

- `MON-122` worker handoff: Research MCP thin batch wrap landed (`run_batch_backtest`, `get_batch_status`, `get_saved_batch_run`). Assets ≤8 after dedupe; compact backend-shaped status/result; partial-asset `errors[]` preserved; `start_ms`/`end_ms` forwarded; no SuiteResult/grid/stress; no backend changes. Full research_mcp suite **67 passed**. Uncommitted — coding manager Accept + commit next.

## 2026-07-09 23:43 EDT

- Folded MON-122 Accept follow-ups into the ticket and closed for real: `get_saved_batch_run` → `saved_runs.py`; batch status `raw` + saved `run_type=batch`; reject assets rows missing `asset_id`; system-tool copy mentions batch. Commit `8f9bec76` on `feat/research_loop`. Linear Done.

## 2026-07-09 23:29 EDT

- Coding-manager accepted `MON-122` (batch wrap only) after deep review + Narrow identity fix. Commit `82fbcd94` on `feat/research_loop`. Tools: `run_batch_backtest`, `get_batch_status`, `get_saved_batch_run`. Post-enqueue poll failures now carry structured `BackendRequestError.run_id`. Linear Done. Non-blocking cohesion follow-ups noted.

## 2026-07-09 23:22 EDT

- Coding-manager launch-gated `MON-122` (respecced batch wrap only): **Ready**. Locked MCP assets ≤8 after dedupe (backend has no hard max). No SuiteResult/grid/stress. Optional `get_saved_batch_run` if clean. Status → In Progress; money-machine-coding worker spawned. Awaiting Linear handoff for acceptance review.

## 2026-07-09 23:12 EDT

- Captured Binance market-data access research in [[vendors/binance-market-data-access|Binance Market Data Access]]. Live US probe: `api.binance.com` / `fapi` → 451; `data-api.binance.vision` spot klines → 200. MM posture: use vision host for backtest/research spot immediacy; trading servers already JP; research backend may need JP (or local VPN) for perp/`fapi` data. Binance.US explicitly out of scope. Renamed `wiki/vendor/` → `wiki/vendors/` because QMD hard-excludes directories named `vendor`.

## 2026-07-10 00:00 EDT

- Locked `MON-132` persistence on the ticket/brief (no migration/code): parent `BtBacktestRun` `run_type=grid` with nullable `grid_spec_json`; real leaf `BtBacktestRun` per cell (single or nested batch); new `bt_backtest_grid_cell` edge table; do not reuse `BtBacktestRunAsset` for cells. Bounds + saved-list default still open for launch review.

## 2026-07-09 23:17 EDT

- Locked `MON-132` V1 input to **cartesian `grid_spec` only** (explicit `cells[]` deferred). Persistence left open for design: same-table parent `run_type=grid` is a lean, but parent→leaf cell edges cannot reuse `BtBacktestRunAsset`. Bounds still open. No worker launch until persistence model is chosen.

## 2026-07-09 22:57 EDT

- Respecced overstuffed `MON-122` into two tickets after Destin ownership decisions: (1) `MON-122` = Research MCP thin wrap of existing backend batch only; (2) new `MON-132` = backend durable grid/suite job (real parent `run_id`) + thin MCP wrap; no UI. Fee/slippage stress deferred to `MON-124`. Updated `MON-122` brief; attached `MON-132` brief. Both Triage pending Destin implied-behavior review. Prior MCP-owned SuiteResult/grid design remains withdrawn.

## 2026-07-09 22:46 EDT

- Deleted accidental `MON-122` Research MCP suite implementation from the working tree (not a git revert of `0f44d698`). Removed `models/suite.py`, suite/batch client tests, and stripped suite tools/validation/allowlist/docs/client batch helpers back to pre-ticket state. Remaining research_mcp smoke/bounds tests **13 passed**. Destin rejected MCP-owned cartesian grid orchestration as the wrong ownership boundary; ticket will be re-read/re-specced backend-first before any re-implementation. Linear stays open / not Done. Working-tree deletion is uncommitted.
- Coding-manager ticket-quality re-read of `MON-122`: **Stop / not launchable**. Core defect is ownership: ticket + brief lock “MCP expands grid; no backend grid/suite job table,” which puts durable multi-config orchestration in the wrong layer. Keepable: batch≠grid identity, no fake parent UUID, no auto-register, bounds. Needs re-spec before any worker.

## 2026-07-09 18:51 EDT

- Completed a one-hour `n_bar_breakout` strategy/research-loop dogfood session. Added typed persistent N-bar breakout code with a 5% default rebalance buffer and 7 passing focused tests. Ran BTC lookback/fee smokes, reviewed saved artifacts, and recorded workflow gaps. A 49-asset UI universe smoke completed 49/49 with 27 positive returns, 26 Sharpe >=1, median return `+7.57%`, median Sharpe `1.30`, and high leveraged risk (median max DD `-21.18%`; 15 assets at/below `-30%`). Results are exploratory only: rolling window, 10x-or-max/default asset settings, no exposed saved batch UUID, and ETH/SOL holdout quarantine consumed.

## 2026-07-09 18:32 EDT

- Created Linear ticket `MON-131` in BackTest v2: Saved-run Trades & Fills Inspector (synchronized chart markers + ledger). Backend-canonical trade/fill grouping; frontend render only; orange = avg position price. Reference AC: 23 fills / 18 trades / 5 rebalances / fees $264.66 / max winner $8,567.16. Status Triage, High priority. Launch gate required before worker.

## 2026-07-08 21:05 EDT

- Coding-manager light re-check of `MON-122` Narrow fix (post-enqueue poll/timeout preserves `backend_run_id` on SuiteResult cells; standalone batch returns one-cell envelope). Decision: **Accept**. Targeted tests 27 passed. Commit `0f44d698` on `feat/research_loop`. Notes: `MON-122-light-recheck.md`, `MON-122-acceptance.md`.

## 2026-07-08 21:00 EDT

- Worker implemented `MON-122` Research MCP suite tools: `run_batch_backtest`, `get_batch_status`, `run_grid_backtest`, `run_fee_slippage_stress_suite` + `SuiteResult` helpers/validation/allowlist/docs. Backend unchanged. Targeted tests 30 passed. Handoff at `mm_v04/.cursor/handoffs/MON-122-handoff.md`. Awaiting coding-manager Accept + commit (no worker commit).

## 2026-07-08 20:50 EDT

- Coding-manager packaged `Brief: MON-122 Batch/grid/suite execution` (Linear doc + local handoff) after worker handback for missing brief. Locked batch→SuiteResult one-cell mapping, assets/cell ≤8, max_cells/variants ≤32, stress `variants[{fees,slippage}]`, best-effort mid-suite failure, `get_saved_batch_run` out, label-only `split`/`purpose`. Removed incorrect Linear `blockedBy: MON-126`. Launch decision: **Ready**. Kickoff at `mm_v04/.cursor/handoffs/MON-122-worker-kickoff.md`. Parent may re-spawn worker.

## 2026-07-08 20:15 EDT

- Coding-manager accepted `MON-130` after narrow recovery follow-up. Committed `6367c63c` on `bug/backtest` (explicit `[start_ms, end_ms)` on single/batch, persist/retrieve, MCP forward, fail-closed asymmetric/incomplete recovery). Marked Linear Done. Migration `c2d8e9f0a1b3` still needs apply per env.

## 2026-07-08 20:00 EDT

- Completed Agentic Research Loop diligence + cohesive implementation plan. Locked shared contracts: research root `wiki/research/trading/`, `project_id`=slug, KB writeback in Research MCP, batch≠grid, registry KB-first, `MON-130` first. Sequencing: `130→128→126→122→(123∥120)→121`; `MON-85` early; v2 `127→124→129→125`. `MON-119` umbrella-only; `MON-130` only Ready implementation ticket. Ticket re-specs in flight. Fixed `wiki/index.md` research routing to `wiki/research/trading/research_process_v1.md`.

## 2026-07-08 20:10 EDT

- Narrow follow-up on `MON-130` after coding-manager **Narrow** review: fail closed on asymmetric persisted range in `compute_retrieval_time_window`; explicit-range hydration completeness via `persisted_retrieval_candles_complete`; `_upsert_run` pairing guard. Focused tests 36/5 passed. Delta handoff posted; awaiting re-acceptance + commit.

## 2026-07-08 19:55 EDT

- Implemented `MON-130` explicit backtest date ranges: additive optional `start_ms`/`end_ms` on single+batch requests, exact-window resolution without warmup expansion, nullable persist/retrieval on `BtBacktestRun`, Research MCP `run_backtest` forwarding, and focused acceptance-matrix tests. Handoff posted on the Linear issue; awaiting acceptance review/commit.

## 2026-07-08 19:35 EDT

- Launch-reviewed `MON-130`. Original ticket left range/`min_candles` relationship, warmup expansion, and persistence shape open. Closed those from live code (`compute_time_window`, half-open candle helpers, `CandleLoadReport`, `BtBacktestRun`, Research MCP `run_backtest`), updated the ticket description, and created issue-linked brief `Brief: MON-130 Explicit backtest date ranges`. Launch decision: Ready.

# Session Change Log

## 2026-07-08 17:15 EDT

- Added urgent Linear child issue `MON-130 Critical: Backtests must support explicit date ranges for research splits` under `MON-119` in the `Agentic Research Loop` project. This captures the hard backend blocker Destin flagged: current single/batch backtest requests only support `interval` + `min_candles`, so runs always use the most recent N bars and cannot target discovery/validation/holdout date windows. `MON-127` remains the later process-enforcement layer, but depends on this backend date-range capability.

## 2026-07-08 16:56 EDT

- Populated Linear project `Agentic Research Loop` with a Research MCP V2 backlog. Created umbrella `MON-119 Research MCP V2: enable end-to-end agentic research loop`.
- Created critical child issues: `MON-128` project state/KB writeback, `MON-126` run registry/config snapshots/artifact references, `MON-122` batch/grid/suite execution, `MON-123` grid summaries/robustness analytics, `MON-120` artifact exports/chart packs/review notes, and `MON-121` handoff/promotion gates.
- Created additional robustness child issues: `MON-127` split/quarantine/holdout enforcement, `MON-124` null/random-entry and standardized baseline support, `MON-129` saved-run metadata filtering/tagging, and `MON-125` research process state machine tool.

## 2026-07-08 16:18 EDT

- Reviewed the Research MCP agent guide/runbook, live MCP tool schema, and `Research Process V1` to identify what is missing for an agent to run the process end to end without ad hoc code. Current MCP supports discovery, single-run backtests, and saved-run/asset hydration, but lacks first-class project/doc writeback, batch/grid execution, run-registry updates, artifact exports, robustness analytics, data split/regime enforcement, baseline/null-run support, and promotion/handoff tooling.

## 2026-07-08 15:58 EDT

- Added per-research-doc folder convention to `wiki/quant/research_process_v1.md`: each research file gets a matching sibling folder (`configs/`, `artifacts/[run-id]/`, `review/`) created together at idea triage, so run/config/artifact links are never placeholders. Clarified that run IDs are the backend's real UUID `run_id`, copied from the run/save response or Research MCP, not invented labels.
- Updated `wiki/quant/research/example_research_doc.md` to match: swapped all placeholder run/batch IDs for realistic UUIDs, nested config/artifact paths under a concrete `research/trend-continuation-slope-filter/` folder, and moved review notes to the top-level `review/` folder per the new convention.

## 2026-07-08 15:50 EDT

- Hardened `wiki/quant/research_process_v1.md` after a review pass focused on overfit controls. Added required up-front data split (discovery/validation/untouched holdout consumed once at promotion), pre-registered period/regime segmentation, run-registry reproducibility fields (backend commit, seed) plus a cumulative search-budget count, a random-entry null baseline, a two-to-three-change modify budget in the reflection step, validation-asset quarantine, and a promotion destination field in the handoff.
- Removed the standalone guardrail threshold list from the optimization-objective step at Destin's direction. Hard reject conditions now live only in research-card kill criteria and promotion gates; section 2 keeps one primary objective, metric definitions, and the objective-change log. Rationale recorded in the doc: fixed threshold lists lose meaning as search size grows.
- Updated `wiki/quant/research/example_research_doc.md` for consistency: guardrails folded into kill criteria, data split and calendar-quarter segmentation in the research card, `run-example-004` random-entry null baseline with registry/appendix links, search-budget count in the run registry, modify-budget line, and an ETH quarantine note.

## 2026-07-08 15:13 EDT

- Rewrote `wiki/quant/research_process_v1.md` into an operational V1 research protocol. Added objective/guardrail definition, baseline runs, required small-grid outputs, formal continue/modify/stop reflection, asset generalization paths, robustness scoring, final optimization loop, candidate promotion gates, and research handoff requirements. Rewrite intentionally stayed scoped to the process doc itself after Destin rejected using the adjacent raw CPO paper as review context.
- 15:22 EDT: Added `wiki/quant/research/example_research_doc.md` as the companion style reference for strategy research docs. It demonstrates stable structured sections plus timestamped write-log and objective-change entries so agents do not silently move goalposts after seeing results. Linked the example from `Research Process V1` and `wiki/index.md`.
- 15:31 EDT: Tightened the research docs around reviewability. `Research Process V1` now requires a run registry, and the example research doc includes concrete saved-run/saved-batch IDs, config snapshot links, artifact-review notes, exported tables, chart packs, and guidance to keep discarded runs visible with exclusion notes.

## 2026-07-08 01:02 EDT

- Created Linear `MON-116 Backtest threshold engines: support typed long/short threshold conditions` in the `Backtesting and Evaluation Correctness` project. The ticket captures Destin's threshold config shape: each long/short entry/exit/stop threshold should become a typed condition object `{ threshold: float, trigger_type: "cross_over" | "cross_under" }`, with trigger type local to the threshold value. Scope is limited to scratch threshold strategies/engines (`StrategyTradeConfig`, `threshold_engine_momo_v1`, `threshold_engine_v2`, `emac_v2`, `emac_v3`) plus focused helper/backtest tests. Launch gate calls out scalar compatibility/default behavior and whether exit/TP stays level-based for gap-through protection.

## 2026-07-07 23:12 EDT

- Implemented local `MON-108 Persist saved-run candle-load diagnostics`. Added nullable JSON snapshots for per-asset `CandleLoadReport` and batch `BacktestBatchCandleLoadSummary`, wired async persistence upserts to store model-dumped diagnostics, and hydrated saved single details, saved batch details, saved batch success/error rows, and saved asset responses back through typed DTO validation. Regenerated the frontend OpenAPI client and wired saved-run Data/drawer views to render persisted diagnostics through the existing candle-load UI. Verification: sandboxed client generation hit the known `.env` permission issue, unsandboxed regeneration succeeded, focused backend compile/tests passed (`8 passed, 1 warning`), frontend lint passes, frontend build passes with existing TanStack/chunk warnings, and Cursor lints are clean.
- Ran coding-manager deep acceptance review on the local `MON-108` dirty diff. Decision: Narrow before acceptance. Backend persistence/hydration direction is sound, but the main Data view can show stale live single-run candle diagnostics for a different selected saved single run, and `BatchResultsSummary` can fall back from explicit saved `null` to unrelated live batch diagnostics. Next action is a narrow frontend state-selection patch, then rerun acceptance review.
- Applied MON-108 narrow acceptance fixes. Main Data view now prioritizes the selected saved single run's persisted `candle_load`, including explicit `null`; `BatchResultsSummary` only falls back to live `batchStatus` when no `candleLoadSummary` prop is provided, so saved legacy `null` rows stay null. Added writer-path tests that execute `AsyncBacktestBackend.save_single_run()` / `save_batch_run()` and assert diagnostics reach asset and batch aggregate upsert payloads. Verification: focused backend diagnostics tests `6 passed, 1 warning`; frontend lint/build pass with existing warnings; Cursor lints clean.
- Re-reviewed updated MON-108 after the narrow fixes. Decision: Accept from coding-manager review standpoint. Fresh verification: sandboxed focused pytest hit the known `.env` permission issue, unsandboxed focused saved candle-load tests passed (`6 passed, 1 warning`), frontend lint passes, frontend build passes with existing TanStack/chunk warnings, and Cursor lints are clean. Not committed.

## 2026-07-07 23:00 EDT

- Re-reviewed the updated local `MON-112` diff after narrow acceptance fixes. Decision: Accept from coding-manager review standpoint. The duplicate chart timestamp blocker is fixed by deduping `chart_ts_ms` in `build_position_chart_points()`, persisted position-event ordering now uses `ts_ms, created_at, id`, and focused regressions cover duplicate scored timestamps plus same-timestamp live-vs-saved builder equivalence. Verification: first sandboxed pytest hit the known `.env` permission error, unsandboxed focused backend tests passed (`6 passed, 2 warnings`), edited backend files compile, Research MCP saved-run client test passed (`7 passed`), frontend lint passes, frontend build passes with existing warnings, and Cursor lints are clean. Created `mm_v04` commit `e2eae6f9 Add backend-owned backtest position charts`.

## 2026-07-07 22:48 EDT

- Ran coding-manager deep acceptance review for local `MON-112 Backtest chart artifacts: backend-owned position/ROE chart series`. Decision: Narrow before acceptance. Core implementation is directionally correct and lints are clean, but the chart-row identity invariant is not yet enforced because `position_chart` cardinality follows raw `chart_ts_ms` / `pnl_points` entries and the frontend renders every point directly. Add a narrow fix/test for unique scored timestamps and harden same-timestamp persisted hydration equivalence before accepting.

## 2026-07-07 22:38 EDT

- Implemented local `MON-112 Backtest chart artifacts: backend-owned position/ROE chart series`. Added additive backend `BacktestPositionChartPoint` / `position_chart` response contract while preserving raw `positions`; derived chart rows through shared `build_position_chart_points()` using scored `pnl_points` timestamps, deterministic last-event-per-timestamp selection, `EXCHANGE_MIN_NOTIONAL_USD` active gating, and flat zero-ROE semantics for missing/closed/sub-threshold rows. Wired live full responses and saved-run asset hydration through the helper without adding persistence, regenerated the frontend OpenAPI client, and switched `TradingViewChart` position/ROE panes to the backend-owned series. Verification: edited backend files compile, focused backend tests `13 passed, 2 warnings` outside sandbox, frontend lint passes, frontend build passes with existing TanStack/chunk warnings, and Cursor lints are clean.
- Made the Research MCP contract explicit for `MON-112`. No new tool or request parameter is needed because Research MCP preserves raw backend responses, but `get_saved_run_asset.artifact_counts` now counts `position_chart`, and the runbook documents that compact mode strips `position_chart` while saved asset hydration exposes it for artifact diagnostics. Focused Research MCP tests passed outside sandbox: `17 passed`.
- Applied MON-112 acceptance patch after coding-manager narrow decision. `build_position_chart_points()` now dedupes repeated scored timestamps so `position_chart` has one row per unique scored-bar timestamp, saved position-event retrieval orders by `ts_ms, created_at, id`, and tests cover duplicate chart timestamps plus saved same-timestamp event selection matching live helper semantics. Verification rerun: focused backend tests `14 passed, 2 warnings`, focused Research MCP tests `17 passed`, frontend lint passes, frontend build passes with existing warnings, edited Python files compile, and Cursor lints are clean.

## 2026-07-07 19:55 EDT

- Ran coding-manager relevance check on Linear `MON-112` after `MON-111` was resolved by the `MON-110` residual-dust close invariant and after `MON-114` sizing floor cleanup. Decision: do not launch `MON-112` as originally written; it is stale as an urgent ROE mismatch fix, but still valid as a lower-priority backend-owned chartable position/ROE series hardening ticket. Current code still has frontend `TradingViewChart` deriving position and ROE panes from raw position events with local latest-event-per-second collapse, while backend metrics use a separate unexposed `BarPositionSnapshot` path and saved-run hydration reloads raw `BtPositionEvent` rows. Tightening should define one chart row per run/asset/scored candle, active/sub-threshold/closed semantics, same-timestamp event selection, persistence/hydration parity, and negative-path tests before launch.
- Rewrote Linear `MON-112` as `Backtest chart artifacts: backend-owned position/ROE chart series`. The new ticket body explicitly frames the work as post-`MON-111` hardening, not the old ROE bug fix, and includes the chart-row contract, semantic invariants, repo anchors, acceptance criteria, anti-goals, and launch note requiring a coding-manager launch review before worker execution.

## 2026-07-07 19:46 EDT

- Closed the `MON-109` warm-up chart discussion as a non-issue from a product/chart interpretation standpoint: early equity weakness can be ordinary start-window variance, and the existing UI can already make warm-up/metric starts clear enough. Created separate Linear follow-up `MON-115 Backtest strategy warm-up bars: audit against full feature lookback requirements`, related to `MON-109` and `MON-99`, to audit backend `warmup_bars` contracts against actual strategy dependencies such as `emac` / `emac_v2` `slow_window`, `sig_extension(..., 100)`, and `instrument_vol_scores(..., long_term_span=200)`.
- Expanded `MON-115` with traced short-history behavior. `sig_extension(signal, 100)` returns raw `signal_value` while bands/stats remain `NaN`; default `MIN_MAX` can still emit decisions, while `PERCENT` can suppress through unusable denominators. `instrument_vol_scores()` computes early base EWMA vol, but 35-sample scores and 200-sample `t_score` are `NaN` until ready; `vol_adj_position()` only uses `r_score` and skips adjustment when it is unavailable. The ticket now asks workers to classify executable-decision dependencies separately from observability/debug fields before changing warm-up semantics.

## 2026-07-07 18:35 EDT

- Started paired MON-99 implementation with a narrow executable-target semantics change. `signal_to_position(..., inverse)` now treats `abs(raw) == 1` saturation as explicit flat and normalizes possible `-0.0` to canonical `0.0` instead of returning signed `POSITION_EPS_USD`. Added focused helper tests for inverse `-1`/`+1` flat-zero behavior, no negative-zero leak, and direct/inverse nonzero sign preservation. Verification: first sandboxed pytest hit the known `.env` permission issue, unsandboxed focused helper/sizing pytest passed (`18 passed, 2 warnings`), edited Python files compile, and Cursor lints are clean.
- Added MON-99 buffer/processing evidence. New buffer tests lock in `CLIP = 0_flat`, `STICKY = previous-sign EPS`, and `STICKIER = last-non-neutral-sign EPS`; new processing tests prove `process_signal_last(...)` matches `normalize_signal(...)[-1]` across `NONE`, `CLIP`, `STICKY`, `STICKIER`, and `SMOOTH`. The test exposed one real mismatch: `normalize_signal()` did not handle `BufferType.NONE` when `SIG_BUFFER > 0`; patched it to pass through like the scalar path. Verification: utils-only signal tests `9 passed, 1 warning`, edited files compile, `diff --check` passes, and Cursor lints are clean.
- Clarified MON-99 min/max scaling semantics. Renamed the implementation to `mag_min_max_scale()` with `min_max_scale()` kept as a compatibility wrapper; `SignalScale.MIN_MAX` now routes to the signed-magnitude scaler. The scaler now returns full-length arrays, computes trailing-window absolute-magnitude min/max bounds, and reapplies original sign so trading direction is preserved in `[-1, 1]`. Updated min/max tests for full-length output, signed direction, all-negative/all-positive windows, trailing-window outlier rolloff, non-finite handling, and wrapper compatibility. Verification: focused signal utility tests `17 passed, 1 warning`, edited files compile, `diff --check` passes, and Cursor lints are clean.
- Added a focused `emac` MON-99 regression fixture. The fixture forces processed signal `-1.0` under inverse sizing and proves the strategy emits the existing `emac_extension` signal but produces no order/fill/position from flat, and closes an existing short to flat rather than carrying a tiny signed target. Verification: focused MON-99/emac suite `27 passed, 2 warnings`, the new test file compiles, `diff --check` passes, and Cursor lints are clean.

## 2026-07-06 22:22 EDT

- Ran coding-manager orientation for Linear `MON-111` without launching another agent or editing `mm_v04`. Read the issue, linked fresh-eyes brief, comments, current checkpoint/changelog, and relevant code paths in mock position emission, backtest aggregation, saved-run hydration, metrics, and `TradingViewChart`. Current assessment: the ticket is ready for an in-chat diagnostic slice, not a blind fix. The strongest code-level gaps are chart/metric active-position gating parity, same-second position/ROE event selection, and saved live-vs-hydrated ordering/field equivalence. Existing tests already cover mock close events, mock leverage/MTM arithmetic, and engine MTM fallback.
- Implemented the first two `MON-111` slices locally. Added backend diagnostics proving margin-based ROE is cleared on close after nonzero MTM and that engine public/retained artifacts select the closed neutral event after pre-decision MTM. Patched `TradingViewChart` so position and ROE panes derive from the same per-second selected position event and charted ROE is neutral for closed, zero-size, or sub-$10 notional positions. Verification: focused diagnostics `2 passed`, frontend lint passes, and frontend build passes with only existing non-fatal warnings.
- Reverted the frontend masking approach after Destin rejected it and added backend `MON-111` lifecycle logs in `mock_services.py` and `bt_engine.py`. Repro run `451f4857-28b4-443c-9d9a-f87b13553900` with BTC `emac`, base DTO configs plus `SIG_BUFFER_TYPE=clip` / `SIG_BUFFER=0.3`, showed the real issue: close events are clean, but sub-min-notional residual `update` events survive as tiny positions with nonzero ROE. Created attached Linear doc `Findings: MON-111 ROE lifecycle vs sub-min residuals` on `MON-111` and read `MON-110`, which owns the near-flat sizing/churn investigation.
- Ran the first `MON-110` investigation/fix slice. Repro comparison showed mock execution leaves rounded close residuals live: e.g. `-0.052 BTC` bought back by `0.05199 BTC` becomes about `-0.00001 BTC`; exact-zero close logic then keeps sub-min-notional dust alive and MTM emits ROE. Added `MockTradingService._snap_dust_transition_to_flat(...)` and a focused regression proving reduce residual dust emits a closed zero-ROE event and clears state. Verification: focused mock/ROE lifecycle tests `10 passed, 2 warnings`; lints clean. Created Linear doc `MON-110 Discovery: Residual Dust Close Invariant` on `MON-110`.
- Completed full post-fix `MON-110` repro verification after restarting the backtest manager. BTC `emac` `SIG_BUFFER_TYPE=clip` / `SIG_BUFFER=0.3` runs now have zero live under-$10 residual updates and zero under-$10 nonzero-ROE rows in both modes: `MIN_ADJUSTMENT_CLIP=true` run `6e40a0d5-3ba6-4a2d-83ab-35e80cfe225c`, `MIN_ADJUSTMENT_CLIP=false` run `36a9d712-ec84-4edf-a27c-9f43c1723202`. Updated the attached Linear discovery doc with the completed verification.
- Posted resolution comments and moved `MON-110` and `MON-111` to Done. Created follow-up `MON-114 Backtest sizing: clarify min-adjustment floor vs flat-target clipping semantics` for the distinct issue where `MIN_ADJUSTMENT_CLIP=false` floors tiny near-flat target drift into forced near-minimum trades (`$19.95` with `$20`, about `$9.96` with `$10`) instead of treating the region as flat.
- Implemented MON-114 locally. Removed `MIN_POSITION_VALUE`, `MIN_ADJUSTMENT_VALUE`, and `MIN_ADJUSTMENT_CLIP` from strategy-facing trade config; helpers now suppress under-threshold targets/deltas instead of promoting them; backtest and production/example strategy callers use pct-only helper signatures. Focused sizing/lifecycle tests pass (`22 passed, 2 warnings`), edited backend files compile, lints are clean, and default `emac` trade config now exposes only pct-based sizing fields. Full Research MCP repro is pending backtest manager restart.
- Follow-up MON-114 repro showed the promotion path was gone and MON-110 residual ROE stayed clean, but pct defaults of `0.0` still allowed raw tiny rebalances, including sub-$10 fills, because sizing did not apply `EXCHANGE_MIN_NOTIONAL_USD` as a suppression floor. Patched `apply_position_limits()`, `calculate_position_delta()`, and `calc_position_delta_size()` so nonzero targets/ordinary adjustments suppress below `max(exchange min, pct floor)` while explicit close-to-flat still passes through. Verification: focused exchange-floor pytest `10 passed, 2 warnings`, edited files compile, and Cursor lints clean. Full post-refinement MCP repro is pending another backtest-manager restart.
- Final MON-114 post-refinement Research MCP repro passed after backtest-manager restart: run `7cdf5504-9156-4077-aba4-045792859c0a` with BTC `emac`, `1m`, `1000`, `SIG_BUFFER_TYPE=clip`, `SIG_BUFFER=0.3`, pct-only trade config. Artifacts: `orders=413`, `fills=413`, `positions=476`, `sub_10_fills=0`, `near_10_fills=0`, `residual_updates=0`, `nonzero_roe_residuals=0`, `closed=6`. Remaining small fills are above the `$10` exchange floor and are raw rebalances, not forced sub-minimum promotion.
- Posted final resolution note on Linear `MON-114` and moved it to Done. Then did a read-only first-pass orientation on `MON-99 Backtest signal semantics: separate raw signal, transformed signal, flat state, and EPS sign`. Created issue-linked Linear document `First pass: MON-99 signal semantics` (https://linear.app/money-machine/document/first-pass-mon-99-signal-semantics-03d8cd7a526d) and linked it in a ticket comment. Assessment: likely semantic collapse between raw signal, transformed signal, EPS direction marker, explicit flat state, and executable target. Recommended first implementation slice is helper tests before UI work.

## 2026-07-04 00:25 EDT

- Applied a narrow `MON-101` frontend fix in `frontend/src/components/Backtest/TradingViewChart.tsx`: USD histogram panes (`Per-candle PnL`, `Position Size`) now color values with `abs(value) < 0.005` as neutral gray so bars align with cent-rounded `$0.00` readouts. ROE and signal panes keep raw sign coloring. Verification: Cursor lints clean for the edited file and `cd frontend && npm run lint` passes.
- Updated Linear `MON-110` acceptance criteria to require checking that near-flat churn and fee-only micro-adjustments do not distort trade outcome metrics (`n_trades`, `win_rate`, avg win/loss, expectancy), with documentation or regression coverage if churn remains visible as fees/costs but excluded from counted trades.
- Ran coding-manager orientation for `MON-111` and related tickets (`MON-98`, `MON-101`, `MON-110`, `MON-104`, `MON-105`, `MON-102`, `MON-106`, `MON-103`). After inspecting the attached screenshot, corrected the framing: this is a large/chunky ROE-vs-position lifecycle mismatch, not primarily a tiny nonzero/dust-position issue. Updated Linear `MON-111` in place to call out the recent mock-service ROE/margin/leverage regression window, require diagnostics for emitted `roe`, `margin_used`, configured leverage, event status, and timestamp-collapse winners, and warn against assuming near-flat churn without proof.
- 00:54 EDT: Created issue-linked Linear doc `Brief: MON-111 fresh-eyes ROE lifecycle investigation` and linked it in a `MON-111` comment. The brief is diagnostic-first and asks the worker to prove which layer creates the mismatch before changing code: mock-service emission, backtest aggregation, saved-run hydration, or frontend timestamp collapse. It treats recent ROE formula/unit changes as intentional unless evidence proves otherwise.
- 01:00 EDT: Created Linear `MON-112 Backtest chart artifacts: backend-owned position and ROE series contract`, related to `MON-111`, `MON-101`, and `MON-110`. This is a follow-up architecture cleanup for backend-owned chartable position/ROE rows so lifecycle-sensitive event selection, active/closed semantics, ROE reset/absence behavior, and saved-run hydration live in backend-owned artifacts instead of frontend raw-event reducers. It should not block `MON-111` unless that diagnosis proves the backend series contract is the only clean fix.
- 01:20 EDT: Investigated committed `MON-100` candle pagination failure after `min_candles=5000`. Git history points to `28b23557 Add backtest candle pagination diagnostics`; latest `e02ec38b` is unrelated chart coloring. Runtime logs show the fail-closed path working (`complete=False`, `actual_bars=5161`, `missing_bars=4839`) because the manager requests a doubled ~10000-bar one-minute grid, while Hyperliquid `candleSnapshot` only exposes the most recent `5000` candles. The MON-100 implementation/tests incorrectly treated `5000` as a historical page size; correction should either cap direct snapshot-backed windows without preexisting DB history or route larger windows through a real historical/backfill source, with a regression for older-than-latest-5000 provider unavailability.
- 01:25 EDT: Applied the immediate MON-100 correction requested by Destin: `backend/app/helpers/backtest.py` `compute_time_window()` now uses one-times required lookback (`required * 1`) instead of doubled lookback (`required * 2`). This makes `min_candles=5000` request about 5000 one-minute bars instead of about 10000. Verification: Cursor lints clean for the helper and targeted `py_compile` passes.
- 01:18 EDT: Created Linear `MON-113 Backtest historical data source: evaluate Binance first` in Triage with Low priority. Scope is future historical backtest data sourcing after confirming Hyperliquid `candleSnapshot` only exposes recent candles; Binance is the first candidate to evaluate. This should not block current backtest correctness, chart, or ROE work.

## 2026-07-04 00:22 EDT

- Created Linear `MON-111 Backtest ROE series appears disconnected from position lifecycle`, related to completed ROE/sizing arc tickets `MON-98`, `MON-103`, `MON-104`, `MON-105`, `MON-106`, and `MON-102`, plus adjacent open tickets `MON-110` and `MON-101`. Uploaded Destin's screenshot as ticket evidence. Scope is backend/artifact lifecycle first: verify ROE samples are gated to the same active-position cutoff/close semantics as position snapshots before considering frontend chart fixes.
- 00:32 EDT: Reviewed recent git history for ROE. Key commits are `0801a380` (mock leverage/margin/ROE accounting), `93c21826` (preserve ROE percent points and redefine `max_position_dd` as worst adverse active-position ROE), and `704eb77f` (pre-decision MTM for current-close sizing). Current likely investigation target is not the ROE formula itself, but per-bar snapshot selection and chart/metric gating: engine aggregation takes `res.positions[-1]`, metrics apply `EXCHANGE_MIN_NOTIONAL_USD` active gating, and the chart maps raw position-event ROE by second without the same cutoff.

## 2026-07-04 00:16 EDT

- Created Linear `MON-110 Backtest sizing: investigate near-flat position churn and color/readout invariants`, related to `MON-101`. This came from reviewing `MON-101` screenshots: initial concern looked like `Per-candle PnL` color failure, but zoomed evidence showed tiny signed position bars and fee-bearing micro-fills. Ticket is investigation-first and explicitly warns not to change shared live-trading position helper semantics without safety analysis. `MON-101` remains open for the narrower chart color/zero-line concern.

## 2026-07-03 22:33 EDT

- 23:42 EDT: Split Linear `MON-101` into two separate chart UX tickets. Updated `MON-101` to focus only on signal panel zero-line/signed-color/flat-vs-near-zero clarity and created related ticket `MON-109 Backtest chart warm-up window: mark data, warm-up, and scored boundaries` for data-start/warm-up/effective-scoring boundary visualization. Posted a split note on `MON-101`.
- Pulled Linear `MON-100 Backtest data fetch pagination: support >2,000 bars with completeness checks` through the Linear MCP after plugin refresh. The issue has no comments/documents, so the ticket body is the current brief. Code investigation found the likely mechanism: backtest `min_candles` permits up to `200_000` and the manager requests a doubled lookback, but the exchange fetch path still makes one `candles_snapshot` call for the gap; DB-first loading only fills after the last DB timestamp and has no interior-gap completeness check. Current response/UI only expose `n_bars`, not requested/fetched/scored range or gap/partial status. Recommended next move: tighten MON-100 brief before cold-start launch with repo anchors, pagination/completeness invariants, 2,001+ and sparse-DB tests, and an explicit reporting contract.
- 22:47 EDT: Re-read Linear `MON-100` and live code paths for the current investigation request. Confirmed no Linear comments/documents, one-shot `CandleSnapshotFetcher.fetch()` behavior, forward-gap-only DB refill, timestamp sort/dedupe without expected-grid completeness checks, and no response/UI fields beyond bar counts for requested/fetched/scored range reporting.
- 23:24 EDT: Implemented MON-100 locally. Added paginated Hyperliquid candle range fetching with `5000` as the per-fetch page cap, DB-first missing-span detection including interior gaps, completeness validation before strategy execution, and candle-load reports for single and batch responses/errors. Backtest UI Data views now show requested/actual/missing/fetched/page/estimated-weight diagnostics. Verification: focused candle/request tests `17 passed`, full isolated `backend/tests/backtest` suite `98 passed`, edited backend files compile, frontend lint passes, frontend build passes with non-fatal bundle warnings, and Cursor lints are clean.
- 23:38 EDT: Ran coding-manager deep review for MON-100 against Linear and dirty files with bug, invariant, and logic-cohesion lenses. Decision: Narrow before acceptance. Core pagination/interior-gap/fail-closed behavior looks sound, but acceptance should fix or explicitly scope gaps around scored range reporting, truncated-page negative coverage, `rows_written` semantics, saved-run diagnostic availability, and duplicated page/weight planning.
- 23:39 EDT: Created Linear follow-up `MON-108 Persist saved-run candle-load diagnostics`, related to `MON-100`. Scope is deliberately post-MON-100: persist/hydrate candle-load reports and batch summaries for saved single runs, saved batch runs, and saved batch asset hydration without changing fetch/pagination/completeness behavior.
- 23:43 EDT: Applied the narrow MON-100 acceptance patch. Candle-load reports now include scored range/count fields, `rows_written` is renamed to `rows_upsert_attempted`, truncated-provider-page coverage proves incomplete pages fail closed, and `backend/app/services/backtest/BACKTEST.md` explicitly marks `CandleLoadReport` as live-response / retained-artifact diagnostics pending `MON-108`. Verification: focused candle/request tests `18 passed`, full isolated backtest suite `99 passed`, frontend lint/build pass, and Cursor lints are clean.
- 23:50 EDT: Re-checked the narrow MON-100 acceptance patch. Decision: Accept from a ticket-local review standpoint; no commit created because Destin asked only for a double-check. Verification rerun: candle-loading tests `7 passed`, candle/request tests `18 passed`, edited backend files compile, frontend lint passes, frontend build passes with only non-fatal chunk/code-splitting warnings, and Cursor lints are clean. Only residual note is duplicated page/weight planning between helper/test code and client fetcher, not blocking MON-100.

## 2026-07-03 17:10 EDT

- Ran coding-manager launch/readiness review for Linear `MON-107 Fix frontend Biome lint workflow and restore lint green`. Decision: ready for paired chat execution, but not strict cold-start worker-ready without a small issue brief because there is no linked Linear document and the current Biome diagnostic count is not yet cataloged. Repo check confirmed the unsafe write-enabled lint script, Biome schema/version drift (`2.2.3` config vs `2.2.4` installed), clean `mm_v04` working tree, and missing local frontend Biome binary.
- 17:35 EDT: Implemented MON-107 locally. Frontend `lint` is now read-only `biome lint`, `lint:fix` is the explicit write-enabled check/fix command, and Biome schema matches `2.2.4`. Made the minimum code fixes needed after narrowing the lint boundary: active-strategy nested hook/radix fixes, small style/type-import warnings, and `SystemControl` useless-else cleanup. Biome config explicitly disables broad pre-existing React/a11y/Tailwind rule families that should be handled separately if desired. Verification: `cd frontend && npm run lint` exits 0 with `No fixes applied`; Cursor lints are clean for edited frontend files.
- 17:55 EDT: Implemented MON-106 locally up to the requested migration stop point. Added first-class backtest exposure/cost decomposition metrics, wired them through DTOs/models/persistence helpers/saved-run hydration/batch aggregates, displayed them in the Backtest Data tab and batch surfaces, and documented/asserted Research MCP passthrough. No Alembic migration file was created. Verification: focused backend decomposition/golden-ledger tests passed outside sandbox (`17 passed, 2 warnings`), Research MCP saved-run tests passed (`7 passed`), edited backend files compile, frontend lint passes, and Cursor lints are clean. Frontend build was blocked by unrelated `monitoring.markets.tsx` type errors.
- 18:00 EDT: Investigated and fixed the frontend build blocker. `monitoring.markets.tsx` `sortValue()` returned optional generated market/technical API fields despite declaring `string | number | null`; missing sort values are now normalized to `null`, preserving the existing comparator behavior. Verification: `cd frontend && npm run build` exits 0 with only non-fatal TanStack Router code-splitting and large-chunk warnings; Cursor lints are clean for the edited route.
- 18:05 EDT: Destin generated MON-106 Alembic migration `bbdb8d9fe923_add_decomposition_metrics_to_` and regenerated the frontend OpenAPI client instead of keeping handwritten generated artifacts. Re-verified combined MON-106 state: focused backend tests `17 passed`, Research MCP saved-run tests `7 passed`, frontend lint passes, frontend build passes, and Cursor lints are clean.
- 18:15 EDT: Revised Linear `MON-103` after Destin clarified the metric should answer "how leveraged can we run this?" rather than peak-to-trough ROE giveback. New contract: replace/label the metric as worst adverse active-position ROE in percent points; `+45.45 -> -9.09` should report `-9.09`, not `-54.54`, and all-positive active ROE should report `0.0` for leverage-risk purposes.
- 18:20 EDT: Ran coding-manager launch review for `MON-103`. Decision: ready for paired execution from the Linear ticket body; no separate issue-linked brief exists, so create/copy one only for a cold-start worker handoff. Repo check confirmed current backend/UI behavior still matches the ticket's target mismatch: ROE is divided by `100.0`, metrics compute peak-to-trough `max_position_dd`, and frontend displays it as a fractional max position drawdown.
- 18:35 EDT: Implemented MON-103 locally. Backtest aggregation now preserves position ROE percent points, `metrics._compute_position_metrics()` uses existing `max_position_dd` as worst adverse active-position ROE capped at `0.0`, and backtest UI labels/formats the field as `Worst Position ROE` without double-scaling. Verification: focused backend metrics test `15 passed`, adjacent metrics/golden-ledger/sizing checks `32 passed`, edited Python files compile, frontend lint/build pass, and Cursor lints are clean.
- 18:40 EDT: Read Backtesting and Evaluation backlog for MON-98 through MON-102 and reran MON-102's current full isolated backtest suite. Current result is `87 passed, 5 failed, 2 warnings`; old missing-crossMargin/string-run-id failures are gone, leaving four EMAC v2 entry/residual behavior failures plus one stale `trade_config` fixture. Recommendation: clear MON-102 next, then close MON-98 if full backtest suite is green.
- 18:50 EDT: Implemented MON-102 locally with test-only stale fixture fixes. EMAC v2 entry tests now set explicit threshold config matching their signal windows, residual close test uses the seeded `88_000` close price so size-based adjustment logic sees the intended notional, and the active-strategy signal config fixture includes `trade_config=None`. Verification: focused failure files `8 passed`, full isolated `backend/tests/backtest` suite `92 passed`, edited tests compile, and Cursor lints are clean.
- 18:55 EDT: Closed out `MON-98 Backtest sizing invariants` in Linear. Posted a rollup comment covering `MON-104`, `MON-105`, `MON-106`, `MON-103`, and `MON-102`, then moved the umbrella issue to `Done`. Follow-ups `MON-99`, `MON-100`, and `MON-101` remain separate backlog work outside the closed sizing-invariants arc.

## 2026-07-03 16:35 EDT

- Tidied the Backtesting and Evaluation Linear board after reading backlog/ready project issues and the prior session logs. `MON-104` is now `Done` with an acceptance comment tied to commit `6f1f8ea1 Add backtest sizing diagnostics support`; `MON-106` is now `Blocked` behind `MON-105`; `MON-98` has a fresh rollup comment. Left `MON-105` in `Backlog` because it still needs coding-manager launch review before it should move to `Ready`; `MON-103` and `MON-102` remain backlog follow-ups.
- Implemented MON-105 locally. `StrategyTradeConfig` now has `SIZING_MODE=compounding|static`, and `BacktestStrategy.current_and_max_position_value()` uses marked current equity for `compounding` or initial capital for `static`. The mode stays in existing `trade_config` JSON, so no DB migration was needed. Backtest UI defaults the trade config editor to `SIZING_MODE=compounding` and keeps the key when mapping backtest configs to active-strategy deployment initial values because live/backtest intentionally share trade config. Research MCP runbook/tests document/pass through `SIZING_MODE` and cover both omitted/default and explicit static payloads. Invalid `SIZING_MODE` now raises through the strategy factory's `ValueError` shape. Added the missing MON-105 acceptance probes: a deterministic static-mode fee-free/no-slippage rebalance fixture proving 100% scales exactly 2x versus 50% for exposure/PnL/metrics, and an EMAC-class probe proving static mode stays anchored to initial capital after 50/100 account equity diverges. Verification: focused backend sizing/golden-ledger tests passed outside sandbox (`17 passed, 2 warnings`), Research MCP focused tests passed (`6 passed`), edited backend files compile, and Cursor lints are clean. Repo-wide frontend lint is currently unsuitable because it auto-writes and fails on pre-existing diagnostics.
- Created Linear `MON-107 Fix frontend Biome lint workflow and restore lint green` in the general Money Machine backlog after MON-105 verification showed `frontend/npm run lint` is write-enabled, mutates unrelated files, has Biome schema version drift, and fails on existing frontend diagnostics.

## 2026-07-02 01:15 EDT

- Investigated MON-98 ROE / max-position-drawdown semantics. Found that `MockTradingService` emits ROE as a margin-based percentage, `_compute_position_metrics()` preserves whatever ROE unit it receives, but `bt_engine._aggregate_backtest_data()` divides emitted ROE by `100.0` before building `BarPositionSnapshot`, making `max_position_dd` fractional while the mock/live ROE contract is percent-based. Existing tests cover emitted mock ROE/margin but not engine-level `max_position_dd` unit propagation. Focused pytest attempt was blocked by sandbox `.env` access during import; no code edits were made.
- 01:24 EDT: Created Linear `MON-103` under Backtesting and Evaluation to align `max_position_dd` with ROE percent-point semantics. Ticket captures the confirmed repo convention (`roe=40.0` means 40%), the `/ 100.0` aggregation mismatch, frontend double-scaling risk, acceptance criteria, regression test shape, and anti-goals.
- 01:52 EDT: Tidied MON-98 in Linear around a proof-first path. Created child issue `MON-104 Backtest golden ledger harness for percent sizing invariants` as the next high-priority proof-gate slice, moved `MON-103` under MON-98 as a narrower follow-up, related MON-103 and MON-104, and added a MON-98 comment clarifying that the harness should come first and drive subsequent poke-hole fixes.
- 15:34 EDT: Pulled Linear `MON-104` with coding-manager launch review. Scope is packageable as a deterministic synthetic golden-ledger backend harness, but launch decision is `Stop` for a cold-start worker until MON-98 ledger items 2/3 are accepted/committed or Destin explicitly launches MON-104 on the current dirty branch. No Linear brief was published because the dependency gate is not clear.
- 15:40 EDT: Destin confirmed MON-98 items 1-3 are committed. Verified `mm_v04` clean at `0801a380 Fix mock backtest leverage accounting`, published Linear doc `Brief: MON-104 Backtest golden ledger harness`, and updated the MON-104 coding-manager launch decision to `Ready`.
- 16:10 EDT: Implemented MON-104 locally with new backend golden ledger harness `backend/tests/backtest/test_backtest_golden_ledger_percent_sizing.py`. The test-local oracle covers fee-free/no-slippage/1x flat -> long -> hold/MTM -> close runs at 10%, 50%, and 100%, asserts public orders/fills/positions/PnL/metrics plus actual-output proportionality, and adds missing-settings/missing-account invariant failures. Verification: focused + adjacent anchor pytest passed (`14 passed, 2 warnings`), py_compile passed, and Cursor lints are clean. No production backtest code was changed.
- 16:52 EDT: Tightened the MON-104 harness after review. Changed the price path to distinct hold and close prices (`100 -> 95 -> 90`), made `total_volume` oracle explicit as open fill notional plus close fill notional, and removed/documented unused ledger-row fields by moving checked account/metric expectations into a summary oracle. Verification remains green: focused + adjacent anchor pytest `14 passed, 2 warnings`, py_compile passed, and Cursor lints are clean.
- 17:25 EDT: Read MCP engineering docs and made a narrow `research_mcp` patch for the real 10/50/100 backtest characterization loop. `run_backtest` now passes through `assets` for per-run settings like `max_position_percent`, and new read-only `get_saved_run_asset(run_id, asset_id)` hydrates artifact-bearing saved single-run asset detail. Updated Research MCP runbook and KB MCP contract/checkpoint docs. Verification: focused Research MCP suite passed outside sandbox (`18 passed`); sandboxed run is blocked by `.env` access.
- 17:42 EDT: Used refreshed Research MCP to run controlled BTC `emac` 10/50/100 backtests with explicit asset settings. Discarded the first 10% run because it used a different 941-signal candle window. Clean runs (`10=4e1f099f-6345-419e-a53e-398fd21a12d3`, `50=8268a502-28e5-490a-9529-156ff6c14084`, `100=b893ef8f-ef43-4b9c-8f82-dae259c5a701`) share identical signals and artifact counts. PnL/return scale close to expected, while order/exposure deltas are nonlinear due to precision/min-adjustment behavior; next probe should isolate EMAC adjustment sizing before metrics.
- 18:07 EDT: Saved the corrected canonical `MON-98` smoking-gun repro. This captures Destin-confirmed settings, run IDs, and UI headline metrics showing 100% total return underperforming 50% while risk/costs rise. The original note is no longer present in the canonical wiki.
- 18:20 EDT: Artifact drill found strategy sizing does receive leverage/position settings, but the running backend emits position artifacts with `leverage_value=1` and `margin_used=value`. Direct local `MockTradingService` sanity check emits `lev_value=10` and `margin_used=value/10`, so the next check is to restart/reload FastAPI and rerun the small full-detail leverage diagnostic.
- 18:24 EDT: Destin found Docker backend was also running and likely serving MCP traffic. After Docker compose down, Research MCP full-detail diagnostic emitted `leverage_value=10` and `margin_used=value/10`, confirming the previous diagnostic/canonical run set was stale Docker-backed. Marked the `MON-98` run IDs as stale pending local-only rerun.
- 18:28 EDT: Reran canonical BTC `emac` 10/50/100 set against local-only backend. Valid run IDs: `10=9596d52b-6d38-4357-858a-97f876eda686`, `50=98369b3f-072e-4476-912d-98492d8eaaf9`, `100=4f636dd5-308f-4891-bc54-9c00d55fb4e1`. Smoking gun persists: 50% total return `20.05%` beats 100% total return `18.69%` while 100% has higher volatility, drawdown, fees, and worse win/in-money rates.
- 18:35 EDT: Drilled into the valid local-only MON-98 canonical BTC `emac` 10/50/100 runs. Hydrated artifacts showed identical signals and correct leverage artifacts, but 100% generated `2.57x` the 50% turnover/fees rather than `2x`. Zero-cost 50/100 isolation removed the return inversion (`26.23%` vs `23.32%`), fee-only reproduced it (`17.31%` vs `19.34%`), and slippage-only did not. Updated the historical `MON-98` note and checkpoint with the current conclusion: fee drag on path-dependent superlinear turnover is the main mechanism behind the smoking gun.
- 18:52 EDT: Completed final MON-98 turnover mechanism drill. Actual 100%/50% exposure ratio tracks compounded equity-base ratio almost exactly (avg about `2.22x` for both); during the profitable middle, 100% equity reaches about `1.323x` 50% equity, creating a `2.645x` max target-base ratio. A fixed `2x` 50% exposure path would imply about `52.52M` signed exposure movement, while actual 100% movement is about `67.56M`; changing compounded multiplier magnitude accounts for about `10.28M` of path movement. Updated evidence/checkpoint.
- 18:56 EDT: Created MON-98 follow-up tickets in Linear: `MON-105 Add fixed-capital sizing mode to backtests` for explicit `current_equity` vs `initial_capital` sizing semantics, and `MON-106 Add backtest exposure and cost decomposition metrics` for backend/saved-run/Data-tab decomposition of exposure, turnover, gross/net returns, fee drag, and cost drag. MON-106 is blocked by MON-105 for clean sequencing.

## 2026-07-01 22:26 EDT

- Ran MON-98 coding-manager orientation/deep dive from Linear: "Backtest sizing invariants: make position % behavior explicit and testable."
- Traced sizing from request asset settings through `BacktestStrategy.current_and_max_position_value()`, strategy signal/vol/limit helpers, mock trading accounting, `bt_engine`, and metrics.
- Identified likely failure modes before narrowing: missing asset-settings fallback to 100% initial capital, implicit compounding from current equity, leverage-as-sizing-only behavior, inconsistent vol/limit ordering across strategies, USD-value vs executable-size helper split, and absolute min/dust thresholds that affect low-exposure runs disproportionately.
- Recommended narrowing MON-98 around a canonical sizing contract plus deterministic 10/50/100 fixtures before broad implementation.
- 22:51 EDT: Created Linear issue-linked document `Brief: MON-98 Backtest sizing invariants` with issue ledger, canonical sizing contract, diagnostic/contract worker slice, coding-manager launch review (`Ready` for the diagnostic slice only), and worker kickoff prompt. Document URL: https://linear.app/money-machine/document/brief-mon-98-backtest-sizing-invariants-363ac35be098
- 23:58 EDT: Implemented MON-98 ledger item 1 as manager-boundary validation: single and batch backtest execution now reject empty/missing/duplicate/mismatched asset settings before settings can reach `BacktestContext`/strategy sizing. Request DTOs remain data shapes. `BacktestContext` now requires `asset_settings`, and `current_and_max_position_value()` raises instead of falling back to 100% initial capital when settings are missing. Focused validation tests passed (`11 passed, 2 warnings`); touched files compile and lints are clean. A broader service test body passed but root DB fixture teardown still has an unrelated portfolio/user FK cleanup issue.
- 00:14 EDT: Ran coding-manager acceptance pass for MON-98 ledger item 1 against Linear ticket/brief and committed diff `b7b46492`. Decision is `Narrow`: behavior matches the missing-settings fallback fix, but the commit includes accidental scratch file `backend/app/services/backtest/Untitled`. Sandboxed pytest re-run was blocked by `.env` permission access during collection; prior focused passing validation remains the current verification baseline.
- 00:42 EDT: Implemented MON-98 ledger item 2 locally. Backtest engine now marks mock portfolio/account state to the current candle close before same-close strategy sizing, preserving post-decision MTM as the emitted no-trade event path. Added deterministic sizing invariant tests for current-close decision-time MTM and marked-current-equity percent scaling; tightened missing `crossMarginSummary` invariant handling. Verification: focused backtest tests passed (`21 passed, 2 warnings`), touched Python files compile, and Cursor lints are clean.
- 00:58 EDT: Implemented MON-98 ledger item 3 locally. `MockTradingService.configure()` now receives required per-asset settings from `BacktestContext`; mock open/MTM accounting emits configured leverage, `margin_used = notional / leverage`, and ROE on margin instead of initial account value. Added focused leverage-accounting tests for open, MTM, and missing-settings failure; updated direct mock-service tests to configure explicit settings. Verification: leverage/accounting focused suite passed (`27 passed, 2 warnings`), touched Python files compile, and Cursor lints are clean. Wider run including EMAC v2 remains affected by pre-existing MON-102 strategy expectation failures.

## 2026-07-01 21:32 EDT

- Reset MON-97 continuity to clean planning/review state after Destin confirmed prior partial work should be discarded.
- Removed tracked leftover test file `backend/tests/backtest/test_backtest_metrics.py`; no MON-97 implementation code was changed.
- Updated `wiki/ops/current-checkpoint.md` to remove stale partial-implementation claims and set next action to fresh coding-manager review and plan.
- 21:50 EDT: Implemented MON-97 narrow fix in `mm_v04`: explicit fill-level `trade_pnls` now drive backtest trade stats, fee-inclusive PnL points/fees remain intact, backtest UI labels now say `Trade Win Rate`, and vectorbt evaluator was only updated for signature compatibility. Verification: edited Python files compile and Cursor lints are clean; targeted pytest could not run because `uv run` cannot spawn `pytest`, and direct smoke execution could not import `pandas` in the current uv environment.
- 21:57 EDT: Corrected MON-97 trade-outcome thresholds to use domain-specific constants: `EXCHANGE_MIN_NOTIONAL_USD` for start-exposure dust and `POSITION_EPS_USD` for gross closed-PnL dollar noise, replacing the initial `DIV_EPS` checks. Recompiled edited backend files and lints remain clean.
- 22:05 EDT: Resolved multi-root workspace uv issue by running backend tests with explicit `--project /Users/destinguarnieri/Desktop/codebase/mm_v04/backend`. Focused MON-97 tests now pass (`8 passed, 2 warnings`). Wider `tests/backtest` run currently has unrelated/out-of-scope failures (`44 passed, 8 failed, 2 warnings`).
- 22:09 EDT: Created Linear `MON-102` under Backtesting and Evaluation to restore the full `backend/tests/backtest` suite to green. Ticket includes the exact reproduction command, focused MON-97 passing baseline, all 8 observed failures, acceptance criteria, and anti-goals.

## 2026-06-29 02:04 EDT

- Normalized copied agent templates into `wiki/agents/templates/` and updated `wiki/index.md` routing for the template directory.
- Updated `mm_v04/.cursor/skills/` pointers away from source-repo `docs/meta/...` paths to Money Machine KB template paths where matching files exist.
- Cleaned stale `docs/meta/...` references inside the agent templates themselves.
- Missing brought-over source docs/templates were identified in `wiki/ops/current-checkpoint.md`; no Money Machine runtime/product code was edited.

## 2026-06-29 01:04 EDT

- Coding-manager orientation resumed MON-97 from dirty files and `wiki/projects/MON-97-backtest-metrics-plan.md`; no `mm_v04` code edits were made.
- Found MON-97 is not acceptance-ready yet: `backend/app/services/backtest/bt_eval_tmp.py` still has a stale positional `compute_perf_metrics` call, and chunk 4's aggregate/engine-level regression is not implemented.
- Verification attempt `PYTHONPATH=/Users/destinguarnieri/Desktop/codebase/mm_v04/backend uv run pytest --confcutdir=tests/backtest tests/backtest/test_backtest_metrics.py -q` failed before collection because `pytest` was not available to `uv run`.

## 2026-06-29 00:05 EDT

- Resumed backtest issue work with read-only orientation on Linear tickets MON-97 through MON-101.
- Inspected backend backtest engine/metrics/mock-service/manager/helper/signal paths and frontend chart route/component surfaces.
- No `mm_v04` code edits were made in this orientation pass; pre-existing PRI local changes remain in the worktree.
- Recommended implementation order: MON-97 metrics denominator first, then MON-98 sizing invariants, MON-99 signal semantics, MON-100 fetch pagination, and MON-101 UX after screenshot/context.
- Added `wiki/projects/MON-97-backtest-metrics-plan.md` with an executable plan for classifying trade outcomes from raw fills before fee-inclusive PnL points collapse the data.
- 00:30 EDT: Completed MON-97 chunk 1 RED tests in `backend/tests/backtest/test_backtest_metrics.py`. Replaced a stale broken `sma_cross` integration test that imported a non-existent module with focused pure metric tests. Verification produced expected RED result: two failures because `compute_perf_metrics()` does not yet accept `trade_pnls`; legacy fallback test passed.
- 00:41 EDT: Completed MON-97 chunk 2. Added optional `trade_pnls` to `backend/app/services/backtest/metrics.py`; when supplied, trade metrics use explicit trade outcomes and ignore fee-only/no-trade PnL points, while legacy `pnl_points` fallback remains. Targeted test file now passes: `3 passed in 0.84s`.
- 00:49 EDT: Completed MON-97 chunk 3. Added `_fill_trade_outcome()` in `backend/app/services/backtest/bt_engine.py` to classify raw fills before PnL-point collapse, collect `trade_pnls` in `_aggregate_backtest_data()`, and pass them into metric computation. Added fill-classification unit coverage. Targeted test file now passes: `6 passed, 2 warnings in 1.50s`.

## 2026-06-28 23:10 EDT

- Verified `price_reversal_v2` had a return-contract bug: the exception path returned `None` despite the `PriceReversalV2` return annotation and callers indexing the result as a dict.
- Fixed `backend/app/lib/signals/pri_v2.py` to return a neutral `PriceReversalV2` fallback with fresh empty list fields on errors.
- Added `backend/tests/indicator/test_pri_v2.py` regression tests for the exception fallback and mutable-list freshness.
- Verification: direct fallback assertion passed; isolated pytest passed with `PYTHONPATH=/Users/destinguarnieri/Desktop/codebase/mm_v04/backend uv run pytest --confcutdir=tests/indicator tests/indicator/test_pri_v2.py`.
- Note: normal pytest invocation hit root `.env` access through repo-level `tests/conftest.py`, so isolated indicator pytest was used to avoid secret-file access.

## 2026-06-28 22:26 EDT

- Created Linear tickets MON-97 through MON-101 for backtesting correctness/UX issues from Destin's backtester complaint list.
- Fixed QMD local install mismatch by reinstalling `@tobilu/qmd@2.5.3` with Bun; verified `qmd status`, `qmd update`, and keyword search work.
- Decided operating model: Money Machine KB is an Obsidian-compatible linked markdown wiki; QMD is indexed search/retrieval over it; prompts enforce discipline/hygiene.
- Patched `~/.hermes/SOUL.md` with global knowledge/continuity discipline.
- Patched `mm_v04/AGENTS.md` with Money Machine KB usage, checkpoint, and changelog requirements.
- Rewrote `/Users/destinguarnieri/Desktop/codebase/mm-knowledge/AGENTS.md` as the KB operating contract.
- Initialized `/Users/destinguarnieri/Desktop/codebase/mm-knowledge/wiki/` with index/log and core context pages.
- Updated canonical checkpoint path: `/Users/destinguarnieri/Desktop/codebase/mm-knowledge/wiki/ops/current-checkpoint.md`.
- Reorganized `/Users/destinguarnieri/Desktop/codebase/mm-knowledge` so canonical category markdown lives under `wiki/` instead of duplicate top-level category folders.
- Moved historical prompt dumps to `archive/prompts_DUMP/` and pointed QMD collections at `wiki/` paths.
- Added `scripts/check_wikilinks.py`; verified current wiki links with `missing=0` and `ambiguous=0`.
- Created Hermes skill `moneymachine-knowledge-base` for Money Machine wiki/QMD workflow.
- Redirected repo prompt/changelog discipline away from `mm_v04/docs/work/*` and into the KB paths under `mm-knowledge/wiki/ops/`.
