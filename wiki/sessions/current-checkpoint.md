# Current Checkpoint

Date: 2026-07-24 02:00 EDT

Company frame: [[company/money-machine-360|Money Machine Operating Context]].

## Active Revenue Proof

- **Objective:** positive net realized live P&L after costs over a founder-set proof period.
- **Proof period:** not yet set; Destin sets it before live evaluation begins.
- **Company phase:** discretionary alpha transfer. Select strategies Destin actually trades, codify their visual and control semantics, prove behavioral parity, and only then validate current economics. Novel discovery is secondary until that inventory is exhausted or Destin explicitly requests it.
- **Strategy / experiment:** the active discretionary-alpha transfer is now Destin's simple HYPE/Hyperliquid/4H EMA high/low 200 re-entry mapping in [[projects/ema_hilo_200_reentry/hype-ema-hilo-200-reentry-codification|HYPE EMA High/Low 200 Re-entry Codification]]. The accepted mapping uses EMA(close, 200) as the direction gate, EMA(low, 10) for long stop/re-entry, EMA(high, 10) for short stop/re-entry, closed bars, and full-equity 1x targets.
- **Observed blocker:** first full-retention run `3a85d0fb-5eb9-4b56-8a4a-0a0e4dcea900` is invalid for economics because the implementation recomputed its full-equity target every candle, creating 1,459 orders over 3,300 scored bars. Destin confirmed fixed entered asset quantity until stop, re-entry, or reversal. The strategy is corrected to transition-only execution; 29 focused tests plus Ruff, formatting, and mypy pass.
- **Next action:** manually restart the local research backtest manager and rerun the identical HYPE/Hyperliquid/4H full-retention fixture. Confirm that orders occur only on `LONG` / `FLAT` / `SHORT` state transitions and compare the event sequence to Destin's charts before interpreting economics or changing parameters. Do not resume the other research tracks while this parity/economics loop is active. Live/capital mutation still needs explicit authorization.
- **WIP:** one primary revenue outcome unless Destin explicitly expands it.

## Current Engineering State

- `MON-122` and `MON-134` are done.
- `MON-132` is blocked behind the backtest-persistence redesign.
- The backtest retention-contract rollout is implemented locally: one request field (`retention_mode=summary|full`), one persisted artifact outcome (`not_requested|writing|available|failed`), durable summaries for both modes, explicit outcome-aware artifact reads, and queryable queue/timeout/cancellation failure states. Legacy request branches, response aliases, writes, and database columns have been removed; unknown request fields are rejected.
- Backtest artifact-completion and hydration are fixed locally: saved-run list/detail queries poll continuously through unchanged `writing` responses, completed batches adopt DB-backed saved detail, and rolling saved runs retrieve candles from the persisted exact first/last bounds rather than a millisecond-offset reconstructed finish window. The chart now hydrates when persistence commits `available`; run `97ec60b1-3bc4-4b3e-92f6-54b01770b7d4` was verified through the API with all 4,000 candles.
- `MON-140` is canceled as superseded by the metrics-only summary retention contract. Long-window `full` retention remains unproven and is deferred until an audit workflow demonstrates that it is a revenue blocker.
- `MON-143` is the independent high-priority cached Binance candle-availability preflight for assigning assets to 5,000/2,500/1,000/<1,000 cohorts before execution.
- `MON-145` is implemented locally and live-verified: a fail-closed backend email allowlist gives configured non-superuser research identities cross-owner read access only on the five saved backtest list/detail/artifact GET routes. Hermes retrieved UI-owned batch `351ecce1-bb07-4b15-82e6-a1a9dccf1fac` and UI-owned single run `b9fab383-5f69-47a3-bd40-f1202bcd8668`; writes, live/capital routes, ownership, and payload contracts are unchanged.
- `MON-146`'s emitted-history contract is restored after a regression exposed by sliding backtest windows. EMAC V4 retains the last two decision-time processed signals per asset/timeframe for Threshold Engine V3; duplicate timestamps are idempotent, older candles are rejected, and fresh strategy instances reset history. Transition-only execution still prevents hold-period resizing.
- `emac_v5` is implemented locally as the continuous-position control requested against V4: it preserves Threshold Engine V3 as the persistent `LONG` / `FLAT` / `SHORT` regime controller, while `signal_to_position` continuously controls magnitude within that regime. Threshold state is independent from actual size so a zero target does not erase it. Focused V4/V5 tests, Ruff, formatting, source mypy, IDE diagnostics, and diff checks pass; no V5 backtest has been run yet.
- The shared signed-magnitude min-max scaler remains prefix-causal and unchanged. That contract does not make upstream EMA history immutable when the backtester's 512-bar input window slides and reseeds the EMA, so threshold consumers must retain prior decision-time emissions at that streaming boundary.
- `MON-141` is canceled: the EMA-session “manual transcription” was agent documentation habit, not a missing report-export product. Research continuity prompting now treats persisted `run_id` + Destin's UI as the metric source; wiki/checkpoint/changelog keep interpretation, and canvas is optional only for unique visuals beyond the UI.
- `MON-85` follows `MON-143` to type and document the complete resulting registered Research MCP surface without changing runtime payloads.
- `MON-135`–`MON-139`, grid expansion, artifact automation, and generalized orchestration are parked until separately justified by evidence from the revenue loop.
- The accepted persistence foundation uses Study → Variant → Trial → Attempt. Batch is an API/enqueue convenience, not a persistence entity.
- The V2 `BtStudy.retention_mode` contract remains independently `metrics|full`; the new `summary|full` vocabulary applies to `BtBacktestRun` requests and was deliberately not propagated into V2.
- Binance spot research candles should use `data-api.binance.vision`; perps require non-restricted egress. See [[vendors/binance-market-data-access|Binance Market Data Access]].
- Destin selected Binance USD-M perpetual history for discovery and recent Hyperliquid candles for venue validation. Local Japan VPN access is verified: Binance futures ping, BTCUSDT 1h klines, and exchange metadata all returned HTTP 200. Idle Japan AWS infrastructure is fallback only.
- The bounded MON-113 implementation is complete: provider-qualified backtest candles and provenance, retry-safe DB-first Binance loading, structured provider failures, research-MCP fields, frontend source/date controls, and `emac_cross`.
- The path now derives direct Binance symbols from selected Hyperliquid assets, accepts exposed timeframes, has no preselected date range, and enforces a 100,000-candle hard cap in both API validation and UI gating. Local migrations are applied through `f8a9b0c1d2e3`.
- Research MCP shared-config batches now accept Binance USD-M, derive each child asset's source symbol independently, preserve partial failures, and accept up to 32 requested assets while backend concurrency remains capped at eight.
- The additive retention migration `b7c8d9e0f1a2` is applied to the local database at Alembic head. Its own downgrade/upgrade executed during the migration round-trip test; the broader historical downgrade remains blocked by populated daily candles that cannot fit the older `SMALLINT` schema.

## Current Decision

Do not continue platform expansion merely because the dependency chain exists. Use the current system to transfer one discretionary strategy at a time: semantic extraction, faithful codification, behavioral parity, then current economic validation. Keep open-ended discovery and the broader Agentic Research Loop parked unless Destin explicitly requests them or codification exposes a causal blocker.

## Verification

- `MON-134` accepted and committed as `5a659548`.
- Foundation verification and detailed ticket history are recorded in `wiki/sessions/session-change-log.md` and the issue-linked briefs.
- Historical-data and strategy semantics were narrowed from grid/platform work to the accepted [[engineering/MON-113-binance-backtest-candles-plan|MON-113 plan]].
- The BTCUSDT 1h network-backed UI path successfully exercised the real gap-fetch/cache/backtest flow over approximately six years.
- Two full-range BTCUSDT 1h Research MCP runs reproduced the autosave failure with approximately 285,000 artifact rows each; both run IDs were absent from saved-run retrieval after rollback.
- Across the six reviewable BTC intervals, active positions were in profit for `77.84%`–`88.21%` of active bars despite only `10.22%`–`28.07%` realized win rates. This shifts the diagnosis from simply weak direction to poor profit retention under the hold-until-opposite-cross rule. Worst position ROE is now evaluated alongside portfolio drawdown.
- Generalization verification passed: all 229 backend backtest tests, five research-MCP tests, focused Ruff/compile checks, frontend lint/build, generated client, browser verification of ETH → ETHUSDT and 4h selection, and Alembic head at `f8a9b0c1d2e3`.
- Binance batch verification passed after correcting saved identity hydration to prefer persisted child candle provenance over mutable current asset metadata: 53 focused backend tests, 13 focused MCP tests, 232 non-destructive backend backtest tests, 70 deterministic MCP tests, focused Ruff/mypy/compile checks, frontend lint/build, generated client, and Alembic head. The legacy migration round-trip was excluded because populated `1d` candles cannot be represented by its `SMALLINT` downgrade; the unrelated password-login test still attempts a fake-host network call.
- Legacy-free retention verification passed across 243 backend backtest tests and 82/83 Research MCP tests, focused Ruff and core mypy checks, frontend lint/build, Python compilation, regenerated OpenAPI client, whitespace checks, offline migration SQL rendering, direct schema inspection, and Alembic head `c8d9e0f1a2b3`. The skipped/failing tests are documented pre-existing blockers: the broad migration test reaches the old daily-candle `SMALLINT` downgrade, and the password-login negative test calls a fake host before rejecting missing credentials. No network-backed backtest, live runtime mutation, trading, deployment, commit, or push occurred.
- The first Binance `4h` asset batch (`3c0f2043-aeac-44da-945f-523965b33c97`) completed 19/20 assets over a common 5,000-candle window. ETH, SUI, ARB, and DOGE cleared Sharpe `1.0`; median Sharpe was `0.438`, median drawdown `-64.38%`, median profit factor `0.951`, median time in money `87.84%`, and median trade win rate `23.73%`. MATIC failed cleanly on missing post-delisting candles.
- A point-in-time Hyperliquid `dayNtlVlm >= $250,000` filter expanded the known universe through Research MCP runs `8c3d5e49`, `c98d9c89`, and `28a4f773`. Across the liquidity-eligible set, 52 assets had complete comparable history and 29 lacked the full 5,000-candle Binance window. ZEC and HBAR posted Sharpe above `1.3` but failed risk review on drawdown, Worst Position ROE, and/or cost drag; no cleaner leader than ETH emerged.
- Tiered-history recovery completed: `20/29` assets ran over a common trailing 2,500-candle window and `7/9` remaining assets ran over 1,000 candles. POL (`1.231` Sharpe, `-40.08%` drawdown, 18 trades) and PAXG (`0.888`, `-26.04%`, 24 trades) led the 2,500 cohort. HYPE, MET, and MON were positive over 1,000 candles but had only 4–8 trades. APEX is unsupported on Binance USD-M; MEGA is 31 candles short of 1,000.
- `MON-146` verification passed: 31 focused strategy/threshold tests, focused mypy, Ruff, formatting, and IDE diagnostics. Full-artifact Research MCP run `bad11f56-d676-4577-92a0-4527b3577f92` confirmed the March 14 crossing flips short to long immediately and completed with 18 trades.
- Causal min-max and unified processing verification passed across 37 focused scaler/processing/V2/V4 tests plus focused mypy, Ruff, formatting, compilation, and IDE diagnostics. Stateless V4 summary run `e43db176-6384-460d-a663-61e2e9f87807` exactly matched the canonical V4 run's metrics, including 18 trades.
- The sliding-window regression was reproduced in BTC 5m run `43036e20-bce1-47d6-88f1-b7cc339f75a9` and corrected in full run `e58f3c5d-e501-4105-9278-f1bcc3ee2b7f`. All four observed missed transitions now execute, and the corrected artifact has no missing, spurious, duplicate, or hold-bar resizing transitions.
- The bounded EMA 10/200 `30m` universe-filter study is closed. Evaluation batch `aa84b35a-1ca6-4776-be07-dfeec643297a` was compared with an immediately preceding disjoint Binance qualification window across 95 assets, using Destin's final winner label of positive return plus Sharpe strictly above `1.0`. Prior winner status and price-only trend/chop features showed only weak persistence; three of nine prior winners repeated, the strongest univariate causal AUC was `0.550`, and the combined prior-winner/low-cross screen captured only 12.5% of evaluation winners. No universe filter is promoted; details and run IDs are in [[research/trading/emac-cross-10-200/emac-cross-10-200|EMA Cross 10/200 Research]].
- The same one-fold screen on direct-position `emac` evaluation batch `eac34bc8-49f6-4a19-b64b-960c08be6862` produced a different provisional lead. Only one of ten prior strategy winners repeated, rejecting prior EMAC performance as a selector. Qualification-window four-bar variance ratio (AUC `0.619`) and one-bar return autocorrelation (AUC `0.593`) were the strongest price-only features; their post-hoc combined median rule selected 38/95 assets at 44.7% precision and 56.7% recall versus a 31.6% base rate. It is frozen as an untouched-window candidate only, not a promoted filter.

## Next Action

Active thread (2026-07-23): **EMAC 10/200 signal-study expansion — escalation ladder across universe & timeframes.** Leg 1 (cross-asset generalization on 5m) **done and accepted**: mean→1σ→2σ ladder replicates across a 20-asset cross-section at 91–98% mean→2σ hit (P(1σ|mean) 0.63–0.71 vs ~0.45–0.54 unconditional). Leg 2 (timeframe axis, BTC) **done and accepted**: conditional structure is timeframe-invariant across `1m`→`4h` (P(1σ|mean) 0.65–0.71, P(2σ|1σ) 0.58–0.66, mean→2σ hit 93–100%) while median mean→2σ payoff scales monotonically `+17 → +404 bps`; naive mean-cycle negative through `4h`; `1d` unreliable (14 legs). Tooling: `run_event_study --asset-id` + `aggregate_escalation.py` (now with `tf_sec` column) in the event-study module. Config/cache run IDs — leg 1: `8077b0dd-…` (5m), batch `c9bdf9fe-…`, ETH `656f267e-…`; leg 2 BTC: `1m 916530d8`, `15m f4cb602b`, `30m 4b8f6e7d`, `1h 3be86756`, `4h 6ffedbdc`, `1d 9c30124d`. Leg-2 tail (non-BTC invariance) **done and accepted**: ran `1m`+`1h` extremes on ETH/SOL/DOGE/AVAX; both BTC signatures replicate on every alt (at `1m` P(1σ|mean) 0.63–0.69, mean→2σ hit 91–96%, +17–30 bps; at `1h` same conditionals, +224–556 bps, higher-vol largest; mean-cycle negative everywhere). Ladder is a joint asset×timeframe invariant. Alt run IDs — `1m`: ETH `9ea7a2cd`, SOL `cb67d560`, DOGE `6a7ebd16`, AVAX `6ccdd9a4`; `1h`: ETH `c3ea4023`, SOL `1d78a2fd`, DOGE `0f3cf6dc`, AVAX `a4afedce`. Collation `panel_tf_extremes_escalation.csv`. Artifacts under `mm_v04/backend/app/lib/analysis/event_study/output/…` + `universe_5m_escalation.csv` / `btc_timeframe_escalation.csv` / `panel_tf_extremes_escalation.csv`. See [[research/trading/emac-cross-10-200/emac-cross-10-200|EMA Cross 10/200 Research]] Write Log 2026-07-23/24. **The escalation-ladder generalization study (legs 1+2) is complete/supported.** **Next research move is a decision, not more coverage:** spec the bounded band-to-band capture variant (S1 entry / escalation-aware / retreat exit) vs the flip-only control, and pick target timeframe(s) given the payoff-vs-fixed-cost scaling (higher TF = far bigger bps/cost margin; `1m` thin but positive). Do not reopen exhaustive universe×TF grids. Live/capital mutation still needs explicit authorization.

Prior active thread (2026-07-21): **`ema_px_trend` improvement loop.** Track 3 (blind chart pattern-match) is complete and codified: strategy `ema_px_trend` in `mm_v04/backend/app/backtest/strategies/ema_px_trend.py`, first backtest ran first try with positive Sharpe on the HYPE 4H dev set (metrics in Destin's backtest UI). Read [[research/trading/ema_px_trend/strategy_ema_px_trend|the strategy doc]] (code + rule semantics + v2 candidates) and [[research/trading/ema_px_trend/blind_pattern_match|the evidence doc]] before touching it. Next: review the run in the UI against the six discriminating events, then improvement work (10 EMA_low integration, hold-confirmation at the line, partial laddering, param tuning, cross-asset/timeframe OOS via Research MCP). An ungraded zero-contamination live call is on record: real short flip unless close-based reclaim of ~64.5 (registered 2026-07-20).

Linear organization (2026-07-22): the `Research` project now has three exclusive `Research Track` labels: Signal Decomposition (MON-153, Backlog), Human-Labeled Charts (MON-152, Blocked on Destin's fixtures/judgment), and Agent Vision (MON-151, In Progress; the sole active item). MON-154 is the later human convergence decision linking all three; it intentionally has no track label because the label group is exclusive. Do not merge the methods into one strategy without compatible, independently evidenced entry, exposure, exit, and failure semantics.

Prior thread (resumed and completed 2026-07-21): the EMAC 10/200 signal-stats **event study**. Observation phase is closed and synthesized; causal event/state labels are frozen in `wiki/research/trading/emac-cross-10-200/event_labels_v1.md` (including the Implementation section), with full session context in `ema_stats_condensed_synthesis_5c512469.plan.md` (same folder — read both before resuming). The `run-event-study` script is built and run on the anchor: `mm_v04/backend/app/lib/analysis/event_study/` (CLI `python -m app.lib.analysis.event_study.run_event_study --run-id <uuid>`; read-only DB, no network). **The event study is complete — anchor and holdout both run and interpreted (2026-07-21).** Verdicts and numbers in `wiki/research/trading/emac-cross-10-200/event_study_anchor_findings.md` (anchor sections + holdout replication section). Holdout: run `9c96c57f-…` (disjoint preceding window Aug 7 2025 → Jan 27 2026, same config), event study rerun via `--run-id 8077b0dd-… --start-ms 1754505600000 --end-ms 1769505600000`. Post-holdout picture: the symmetric magnitude family replicated (escalation conditionals stable at 0.69/0.63; per-rung traversal positive at 88–96% hit; mean-break leg selection +31/−31 bps; mean-in/mean-out cycle regime-stable at +11–13 bps gross while hold-to-flip collapsed to +0.9); rolling opposing-2σ weakened to directional-only; asym handoff, chop flags (200-bar lookback caveat noted), and asym breakout are dead. Nothing clears the ~20 bps flip cost floor standalone.

Post-review state (2026-07-22, Destin accepted findings as good/transferable): cost floor reframed — 20 bps was the max-taker control assumption, maker ≈ 1 bps/side, at which the surviving band-to-band and mean-cycle structures are net positive as measured; all-in/all-out flip positioning is the control only, not the endstate. Compression flags visually examined via the new `plot_compression.py` CLI (event-study module; any date window): `sym_compression` is a ~50%-duty-cycle lag artifact (double-smoothed baseline), width pinch is two-sided (squeeze-before-breakout as well as braid) — the chop-split null is explained, not just observed. Destin annotated the real compression zones on the sym 4-panel chart; the pending candidate semantics (recorded in the findings page) are envelope *level* low + envelope *contracting*, as a standalone market-state label independent of the zero-flip method. A label/metric legend was added to the findings page and the results canvas.

Open items for next session, in order:

1. Destin to phrase the corrected compression/expansion semantics (annotation review in findings page is the starting point); then redraw the flag on the plots, rerun the B1 chop split, only then consider a suppression rule.
2. Destin has an unspecified "slice the data differently" visualization request he was still phrasing — ask him.
3. New untested candidate (Destin, 2026-07-22): breakout episode on the **rolling** stats — enter on rolling ±2σ breach, exit on close back inside (analogue of the dead asym A2→A4 mode). Episode machinery exists; needs a rolling-lens episode builder + economics on both fixtures.
4. Standing decision: whether to spec the bounded mean-cycle strategy variant (S1 entry, retreat exit, optionally escalation-aware) vs the flip-only control.

Pickup facts:

- Primary anchor `8077b0dd-e440-48d7-8e64-a4ef81d1074e` (BTC Binance USD-M 5m, 49,800 scored bars, ~Jan 28 → Jul 19 2026); its 50,000 candles are cached in `backtest_candle`. Hyperliquid companion `d82eda65-…` (full retention) exists for UI/screenshot parity.
- Destin accepted the anchor's brutal flip-only control economics as expected (5m was already a rejected interval for flip-only); the interesting confirmed fact is time-in-money holding ~80% over 49.8k bars.
- Hyperliquid serves only ~5,000 candles per timeframe; Binance USD-M is the depth source (explicit start/end, 50k single-run cap, non-US egress).
- Known label caveats for interpretation: `sym_mean_filter` is near-degenerate at B1 (use `rolling_width`/`sym_compression` for H4); slope-based labels flatten to 0 during negative signal stretches (lr_slope ln-clamp). Destin corrections (2026-07-20): sym #1 means the mean break **as the entry trigger** (the S1 event), and the observation window was not a favorable one.
- **Slope code divergence (2026-07-22):** the live/backtest slope no longer matches the event-study slope. `emac_v4.py` was switched to a new `sig_slope` (raw linear regression, no `ln`) in `backend/app/lib/indicators/slope.py` (added `sig_slope` + `raw_lr_slope`; log `Slope`/`lr_slope`/`ln` untouched, TA-Lib CORREL/STDDEV kept unpadded on purpose — matches Pine `nz()`). The event-study path `backend/app/lib/analysis/event_study/series.py` still uses the **log** `Slope` on the signed signal, so B3/`slope_state` and R5/mean-slope in the findings are legacy ln-clamped and no longer reflect the live strategy. To realign, switch `series.py` to `sig_slope` and rerun the event study; all non-slope labels are unaffected. Flagged in `event_study_anchor_findings.md` Caveats. Not yet changed in research — scoped to live signal this session.
- The three-agent review docs and synthesis are untracked `.plan.md` files in `mm-knowledge` git; the event-study code in `mm_v04` is also uncommitted; no commit authorized yet.

Longer frame unchanged: the bounded EMA V4 control experiment (static thresholds, dynamic signal-statistic thresholds, continuous position semantics) with the positioning-mechanism inventory recorded on the EMA 10/200 durable page. Live/capital mutation still needs explicit authorization.

Researcher reminder: Destin reports [[trading/dump|Trading Knowledge Dump]] is about `70%` complete as of 2026-07-18. Treat it as useful provisional source material, not a finished spec, and keep asking at the start of research sessions until he confirms it is complete.
